[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sentinel-hunter"
version = "0.1.0"
description = "Sentinel Hunter Sidecar Service"
requires-python = ">=3.11"
dependencies = [
    "playwright==1.44.0",
    "beautifulsoup4==4.12.3"
]
