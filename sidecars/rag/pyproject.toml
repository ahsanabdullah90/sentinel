[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sentinel-rag"
version = "0.1.0"
description = "Sentinel RAG Sidecar Service"
requires-python = ">=3.11"
dependencies = [
    "chromadb==0.5.0"
]
