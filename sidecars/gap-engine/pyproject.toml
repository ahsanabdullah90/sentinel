[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sentinel-gap-engine"
version = "0.1.0"
description = "Sentinel Gap Engine Sidecar Service"
requires-python = ">=3.11"
dependencies = [
]
